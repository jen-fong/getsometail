{{#each dogs }}
    <div class="col-md-2">
            <a href="#" class="img-circle">
                <img src="../assets/images/thumbnails/{{ image }}.png" alt={{ breed }}>
                <div class="caption text-center">
                    <button type="button" class="btn btn-primary-topNav hvr-float-shadow dog" data-breed={{ breed }} data-toggle="modal" data-target="#modal-2">{{ breed }}</button>
                </div>
            </a>
        </div>
{{/each }}
    </div>
</div>    
    <div class="row bottom"> 
        <div class="col-xs-4">
           <div id="title" class="span1">{{ girlHeading.heading1}} <span>{{ girlHeading.heading2 }}</span> {{girlHeading.heading3}}</div>
            <p>{{ girl.description }}</p>
        </div>

